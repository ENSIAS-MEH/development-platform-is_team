# Redis Deployment — cache en mémoire
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: mentorpath
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
# Service — expose Redis aux autres pods
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: mentorpath
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379