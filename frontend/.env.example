# Copier en .env : cp .env.example .env
# /api utilise le proxy CRA → http://localhost:8080 (évite les erreurs CORS)
REACT_APP_API_URL=/api
# Requis en dev local avec proxy si webpack-dev-server échoue sur allowedHosts
DANGEROUSLY_DISABLE_HOST_CHECK=true
